# KoshalCity Environment Configuration
# When starting in production mode (e.g. `python run.py prod` or `APP_ENV=prod`),
# settings in .env take precedence.

APP_ENV=production
SECRET_KEY=change-me-in-production-koshalcity-secret
FRONTEND_URL=http://localhost:5173
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database configuration (Hostinger MySQL)
DB_HOST=srv1953.hstgr.io
DB_PORT=3306
DB_USER=u554759618_tradesetup
DB_PASSWORD=oO3O3N4:a?
DB_NAME=u554759618_tradesetup

# Or provide a full custom database URL (PostgreSQL, MySQL, SQLite, etc.)
# DATABASE_URL=mysql+pymysql://u554759618_tradesetup:oO3O3N4%3Aa%3F@srv1953.hstgr.io:3306/u554759618_tradesetup?charset=utf8mb4

# Redis (optional for local/single-process; pubsub falls back to in-memory)
REDIS_URL=redis://localhost:6379/0

# Fast2SMS OTP configuration (optional; leave blank for demo OTP)
FAST2SMS_API_KEY=
FAST2SMS_OTP_ID=
FAST2SMS_OTP_EXPIRY_MINUTES=10
